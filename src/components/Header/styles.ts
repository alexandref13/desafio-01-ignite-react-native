import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  header: {
    height: 173,
    width: '100%',
    alignItems: 'center',
    backgroundColor: '#0D0D0D',
  },
  logo: {
    marginTop: 48,
    height: 40,
    width: 110,
  },
});
